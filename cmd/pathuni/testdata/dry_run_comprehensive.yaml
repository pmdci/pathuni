all:
  paths:
    - "/tmp/pathuni/usr/local/bin"  # Found + untagged (test path)
    - path: "/tmp/pathuni/usr/bin"
      tags:
        - essential
        - system
    - "/garbage-fake-dir-xyz123"  # Not found + untagged
    - path: "/another-fake-dir-abc456"
      tags:
        - missing
        - test

macos:
  paths:
    - "/tmp/pathuni/tmp"  # Found + untagged (test path)
    - path: "/tmp/pathuni/bin"
      tags:
        - macos
        - core
    - "/fake-macos-app-dir-xyz"  # Not found + untagged
    - path: "/fake-macos-tagged-dir-abc"
      tags:
        - missing
        - work

linux:
  paths:
    - "/tmp/pathuni/tmp"  # Found + untagged (test path)
    - path: "/tmp/pathuni/bin"
      tags:
        - linux
        - core