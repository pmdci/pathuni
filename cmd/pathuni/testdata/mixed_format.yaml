all:
  paths:
    - "/tmp/pathuni/usr/local/bin"  # Plain string - test path
    - path: "/garbage-fake-shared-bin-xyz789"
      tags:
        - shared
        - common

macos:
  paths:
    - "/tmp/pathuni/usr/bin"  # Plain string - test path
    - path: "/garbage-fake-development-bin-abc123"
      tags:
        - dev
        - home
    - path: "/garbage-fake-gaming-bin-def456"
      tags:
        - gaming
        - home
    - "/tmp/pathuni/bin"  # Another plain string - test path