all:
  paths:
    - "$HOME/.local/bin"  # Untagged user bin
    - path: "${USER}/bin"
      tags:
        - personal
        - user
    - "/tmp/pathuni/usr/local/bin"  # Untagged system path

macos:
  paths:
    - path: "$HOME/.cargo/bin"
      tags:
        - rust
        - dev
    - "/tmp/pathuni/opt/homebrew/bin"  # Untagged test path
    - path: "${HOME}/.node_modules/.bin"
      tags:
        - nodejs
        - dev
        - local